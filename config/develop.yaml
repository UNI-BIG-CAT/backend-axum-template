http_port: 3000

# 日志配置
log_level: debug # 日志级别 info, debug, warn, error, trace, off
save_log: false # 是否保存日志
log_file: logs/template_detector.log # 日志文件

# redis配置
main_redis:
  ip: 127.0.0.1
  port: 6380
  auth: lktspacE123
  db: 1

# postgres配置
postgres:
  ip: 127.0.0.1
  port: 5432
  db: template_db
  admin: template
  password: template123
  min_connections: 1
  max_connections: 10
  sqlx_logging: true

# 用户配置
admin:
  expires_in: 7 # 用户token过期时间 单位：天
